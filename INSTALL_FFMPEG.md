# 🎬 FFmpeg Installation για Windows

## 🚀 Γρήγορη Εγκατάσταση (3 τρόποι)

### Μέθοδος 1: Winget (Συνιστάται - Windows 10/11)

Αν έχεις Windows 10/11, άνοιξε PowerShell ως **Administrator** και τρέξε:

```powershell
winget install FFmpeg
```

Αυτό θα εγκαταστήσει το FFmpeg αυτόματα! ✅

---

### Μέθοδος 2: Chocolatey

Αν έχεις Chocolatey εγκατεστημένο:

```powershell
choco install ffmpeg
```

---

### Μέθοδος 3: Manual Download (Αν οι άλλες δεν λειτουργούν)

1. **Κατέβασε το FFmpeg:**
   - Πήγαινε: https://www.gyan.dev/ffmpeg/builds/
   - Κάνε click στο **"ffmpeg-release-essentials.zip"** (τελευταία έκδοση)

2. **Αποσυμπίεσε:**
   - Extract το zip σε `C:\ffmpeg`
   - Βεβαιώσου ότι το `ffmpeg.exe` είναι στο `C:\ffmpeg\bin\ffmpeg.exe`

3. **Πρόσθεσε στο PATH:**
   - Windows + R → `sysdm.cpl` → Enter
   - Advanced → Environment Variables
   - User variables → Path → Edit
   - New → `C:\ffmpeg\bin`
   - OK → OK → OK

4. **Επαλήθευση:**
   - Άνοιξε **ΝΕΟ** PowerShell/CMD
   - Τρέξε: `ffmpeg -version`
   - Αν δεις την έκδοση, είναι έτοιμο! ✅

---

## ✅ Έλεγχος

Μετά την εγκατάσταση, άνοιξε **ΝΕΟ** terminal και τρέξε:

```powershell
ffmpeg -version
```

Αν δεις output με την έκδοση, τότε είναι έτοιμο! 🎉

---

## 🎯 Τι κάνει το FFmpeg;

Το FFmpeg μετατρέπει αυτόματα τα videos σε MP4 (H.264) για:
- ✅ Καλύτερη browser compatibility
- ✅ Μικρότερα file sizes
- ✅ Καλύτερη ποιότητα
- ✅ Αυτόματη μετατροπή κατά το upload

---

## ⚠️ Σημαντικό

Μετά την εγκατάσταση, **κάνε restart** το Next.js server:

```powershell
# Stop το server (Ctrl+C)
# Start ξανά
npm run dev
```

Αυτό είναι απαραίτητο για να αναγνωρίσει το FFmpeg!



## 🚀 Γρήγορη Εγκατάσταση (3 τρόποι)

### Μέθοδος 1: Winget (Συνιστάται - Windows 10/11)

Αν έχεις Windows 10/11, άνοιξε PowerShell ως **Administrator** και τρέξε:

```powershell
winget install FFmpeg
```

Αυτό θα εγκαταστήσει το FFmpeg αυτόματα! ✅

---

### Μέθοδος 2: Chocolatey

Αν έχεις Chocolatey εγκατεστημένο:

```powershell
choco install ffmpeg
```

---

### Μέθοδος 3: Manual Download (Αν οι άλλες δεν λειτουργούν)

1. **Κατέβασε το FFmpeg:**
   - Πήγαινε: https://www.gyan.dev/ffmpeg/builds/
   - Κάνε click στο **"ffmpeg-release-essentials.zip"** (τελευταία έκδοση)

2. **Αποσυμπίεσε:**
   - Extract το zip σε `C:\ffmpeg`
   - Βεβαιώσου ότι το `ffmpeg.exe` είναι στο `C:\ffmpeg\bin\ffmpeg.exe`

3. **Πρόσθεσε στο PATH:**
   - Windows + R → `sysdm.cpl` → Enter
   - Advanced → Environment Variables
   - User variables → Path → Edit
   - New → `C:\ffmpeg\bin`
   - OK → OK → OK

4. **Επαλήθευση:**
   - Άνοιξε **ΝΕΟ** PowerShell/CMD
   - Τρέξε: `ffmpeg -version`
   - Αν δεις την έκδοση, είναι έτοιμο! ✅

---

## ✅ Έλεγχος

Μετά την εγκατάσταση, άνοιξε **ΝΕΟ** terminal και τρέξε:

```powershell
ffmpeg -version
```

Αν δεις output με την έκδοση, τότε είναι έτοιμο! 🎉

---

## 🎯 Τι κάνει το FFmpeg;

Το FFmpeg μετατρέπει αυτόματα τα videos σε MP4 (H.264) για:
- ✅ Καλύτερη browser compatibility
- ✅ Μικρότερα file sizes
- ✅ Καλύτερη ποιότητα
- ✅ Αυτόματη μετατροπή κατά το upload

---

## ⚠️ Σημαντικό

Μετά την εγκατάσταση, **κάνε restart** το Next.js server:

```powershell
# Stop το server (Ctrl+C)
# Start ξανά
npm run dev
```

Αυτό είναι απαραίτητο για να αναγνωρίσει το FFmpeg!




